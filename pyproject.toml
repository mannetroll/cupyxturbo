[project]
name = "mannetroll-cupyxturbo"
version = "0.1.5"
description = "2D Turbulence (SciPy + CuPy)"
readme = "README.md"
requires-python = ">=3.13,<3.14"
license = { file = "LICENSE" }

authors = [
  { name = "Torbjörn Sjögren" }
]

dependencies = [
  "numpy>=2.4.4,<3",
  "numba>=0.65.0,<1",
  "matplotlib>=3.10.8,<4",
  "Pillow>=12.2.0,<13",
  "scipy>=1.17.1,<2",
  "PySide6>=6.11.0,<7",
]

# Optional GPU extra
[project.optional-dependencies]
cuda = [
  "cupy-cuda13x>=13.6.0",
  "fastrlock>=0.8",
]

[project.scripts]
turbulence = "scipyturbo.turbo_main:main"
sim = "scipyturbo.turbo_simulator:main"
fps = "scipyturbo.turbo_simulator_fps:main"
cufft = "scipyturbo.turbo_simulator_cufft:main"
opt = "scipyturbo.turbo_simulator_opt:main"

# Package discovery
[tool.setuptools.packages.find]
include = ["scipyturbo"]

[tool.setuptools.package-data]
scipyturbo = ["*.icns"]

[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[dependency-groups]
dev = [
    "build>=1.3.0",
    "twine>=6.2.0",
]
